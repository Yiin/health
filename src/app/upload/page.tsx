import { PlaceholderPage } from "@/components/placeholder-page";

export default function UploadPage() {
  return (
    <PlaceholderPage
      title="Upload"
      description="Drop lab PDFs, wearable CSVs, scans, or export archives — everything is classified and extracted automatically."
    />
  );
}
