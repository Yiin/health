import { PlaceholderPage } from "@/components/placeholder-page";

export default function DocumentsPage() {
  return (
    <PlaceholderPage
      title="Documents"
      description="Searchable library of every uploaded file, with AI summaries and ingestion status."
    />
  );
}
