// Database schema for the health dashboard.
//
// Empty on purpose: domain tables land in their own follow-up tasks
// (labs, documents, activity). Table definitions go here (or in modules
// re-exported from here) so that `npm run db:generate` picks them up.
export {};
